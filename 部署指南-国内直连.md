# 部署指南 · 国内可直接访问

本项目 = 一个 Vite 前端静态页 + 一个 `/api/chat` 服务端代理（服务端持有 DeepSeek 密钥，前端拿不到）。
`server.mjs` 用一个 Node 进程同时托管两者，**零第三方依赖**，任何云主机 / VPS 上都能跑。

> DeepSeek（`api.deepseek.com`）是国内公司，大陆机房直连没问题，所以只要网站本体能在国内打开，游戏就能玩。

---

## 一、先选机器（关键：备案）

在中国大陆，**用「域名 + 大陆服务器」对外提供网页，法律上必须先 ICP 备案**（用身份证/公司，约 1–3 周）；没备案，大陆机房会封掉域名的 80/443 访问。

| 方案 | 备案 | 国内访问 | 适合 |
|---|---|---|---|
| **① 腾讯云 / 阿里云「香港」轻量应用服务器** | ❌ 不用 | ✅ 一般能直连（略有延迟） | **最快上线、放简历，先走这条** |
| ② 大陆服务器 + ICP 备案 + 绑域名 | ✅ 要 | ✅✅ 最稳最快 | 长期正式运营 |

两种机器**部署步骤完全一样**，区别只在于「大陆机器要先备案 + 用域名」。下面以最省事的**香港轻量服务器**为例。

买机器时选：**Ubuntu 22.04**、最低 1 核 1G 即可（2 核 2G 更稳）。

---

## 二、部署步骤

### 1. 本地打包
```bash
npm install
npm run build          # 生成 dist/
```

### 2. 把这些传到服务器
只需要这几样（用 scp / FileZilla / 宝塔面板都行）：
```
dist/            # 打包产物
server.mjs       # 自托管服务
package.json
```
> 传到服务器上某个目录，例如 `/root/idol`。

### 3. 服务器上装 Node 18+
```bash
# Ubuntu，装 Node 20 LTS
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs
node -v                # 确认 >= 18
```

### 4. 配置密钥并启动
```bash
cd /root/idol
export DEEPSEEK_API_KEY=sk-你的DeepSeek密钥
PORT=8080 npm start
```
浏览器访问 `http://服务器IP:8080` 能打开就说明成功了。
（记得在云控制台的**安全组/防火墙**放行你用的端口，比如 8080。）

### 5. 用 pm2 常驻（关机/掉线自动拉起）
```bash
npm install -g pm2
cd /root/idol
DEEPSEEK_API_KEY=sk-你的密钥 PORT=8080 pm2 start server.mjs --name idol
pm2 save
pm2 startup            # 按提示复制执行那行，做成开机自启
```
常用命令：`pm2 logs idol`（看日志）、`pm2 restart idol`（重启）、`pm2 stop idol`（停）。

---

## 三、（可选）用 80 端口 / 绑域名 / 上 HTTPS

想让别人直接访问 `http://你的域名` 而不是带 `:8080`，加一层 Nginx 反代：

```bash
apt-get install -y nginx
```
新建 `/etc/nginx/sites-available/idol`：
```nginx
server {
  listen 80;
  server_name 你的域名或IP;
  location / {
    proxy_pass http://127.0.0.1:8080;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_read_timeout 70s;      # 给慢生成留足时间（>55s）
  }
}
```
```bash
ln -s /etc/nginx/sites-available/idol /etc/nginx/sites-enabled/
nginx -t && systemctl reload nginx
```
- **HTTPS**：域名已解析到本机后，用 `certbot --nginx` 一键签免费证书（大陆机器需先备案，域名才能正常解析访问 80/443）。
- 大陆机器用域名对外访问 = **必须先完成 ICP 备案**。

---

## 四、更新版本
以后代码有更新，只要：
```bash
# 本地
npm run build
# 传新的 dist/ 覆盖服务器上的 dist/
# 服务器上
pm2 restart idol
```

---

## 备注
- 前端调用的是相对路径 `/api/chat`，所以**同一个域名/端口既出页面又出接口**，无需跨域配置。
- 密钥只存在服务器环境变量里，不会进前端包；玩家也可以在游戏里自填自己的密钥（会随请求带上，优先于服务端密钥）。
- 这套 `server.mjs` 同样能跑在任何支持 Node 的地方（阿里云函数计算、Render、Railway、自己的 NAS……），换平台不用改代码。
