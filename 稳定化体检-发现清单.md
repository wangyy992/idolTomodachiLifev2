# 稳定化体检 · 发现清单

**核心原则（抄自 Stardew / Tomodachi Life）**：
游戏掌管「时钟」和「世界状态」，AI 只负责「演当下这一刻」+ 提出小幅改动（好感±、一句记忆、可选的社媒）。
凡是 AI 抢了游戏该管的东西、或状态只靠 AI 回显的地方，都是要堵的漏。

参考：Stardew Valley（时间玩家推进、对话是瞬间、好感+地点/时间→心动事件）、Tomodachi Life（check-in→观察自发事件→满足请求→循环）。

---

## 🔴 一类：时间 / 世界所有权漏洞（"按时刻发生"的定盘星）

**F1. AI 自己跳时间/换场（已修，PR#5）**
旧 prompt 每 2 轮硬命令"结束场景+跳周"。已改为实时对话指令。✅

**F2. `currentScene` 有两个真相源** —— 世界地图有 `worldLocation`（你实际在哪），AI 又每轮用 SNAPSHOT.currentScene 覆盖 `gameState.currentScene`。你在咖啡厅，AI 说"练习室"，顶栏和地图就对不上。
→ 修：沙盒里地点以 worldLocation 为准；AI 的 currentScene 不再覆盖世界地点（顶栏显示由世界决定）。

**F3. 两个时钟打架** —— 叙事时间锚点用的是 `turnCount`（每回复 +1），世界时间是 `worldDay/worldSlot`（玩家推进）。prompt 里写"当前第N轮（唯一时间锚点）"，聊 3 句 turnCount+3，AI 以为过了 3 段时间。
→ 修：喂给 AI 的时间锚点改成"第D天·上午/下午/晚上"（worldDay/worldSlot），并说明"这一刻在对话里固定不变"。turnCount 只当内部计数，不当时间。

**F4. `isComebackSetting` 由 AI 每轮说了算** —— 回归期本该由日历（phaseAt/calendar）决定，现在 AI 的 SNAPSHOT 每轮覆盖它，和日历打架。
→ 修：回归期以日历为准，AI 不再设置它（从 SNAPSHOT 里去掉，或忽略）。

**F5. 打歌名次双源结算** —— 代码在 handleAdvanceTime 里算一位（对），但 prompt 仍允许 AI 输出 MUSICSHOW 块、processAIResponse 也照单全收 push 进历史 → 一位可能被 AI 和代码各算一次、结果冲突。
→ 修：打歌名次只由系统算；从 AI 输出格式里删掉 MUSICSHOW，processAIResponse 不再接受 AI 的打歌结果。

---

## 🟠 二类：玩家能动性 / 照看循环的摩擦

**F6. 行动点：每时段只能深入互动 1 次** —— 满地图需求气泡，你却只能点 1 个，其余只给"闲聊"。和 Tomodachi"逐个照看"的循环直接打架（Stardew 里每天能跟所有人说话）。
→ 修：放宽——要么取消全局单次、改成"每个爱豆每时段 1 次"，要么干脆去掉这个闸门（自带 key 的玩家本就无限）。建议改成按人计。

**F7. 行动点在 AI 成功前就扣了（todolist #22）** —— consumeAction 在 await 之前就写 actionUsedAt；若这次请求失败（SSL/超时），这一时段的机会白白烧掉。
→ 修：AI 成功返回后再扣。

---

## 🟠 三类：状态可靠性（"只靠 AI 回显"一漏就崩）

**F8. 好感只走 SNAPSHOT（todolist #23）** —— AI 漏写/写坏 SNAPSHOT，这一轮好感就不更新，长期停滞。
→ 修：加兜底——有实质互动至少 +1；或从正文/选择推断一个保底增量。

**F9. 曝光度只增不减（todolist #24）** —— 只在 RISK 块里涨，从不回落，是一路奔向 BE 的棘轮。
→ 修：每推进一天，低调无事就自然回落一点。

**F10. 里程碑/事件去重靠 AI 回显 ID（todolist #25）** —— 靠 AI 原样输出 MILESTONE_ID/EVENT_ID 才记录"已触发"。AI 忘了回显，重头戏就会反复触发（现在里程碑是加长大戏，重复=很糟）。
→ 修：触发条件命中即在客户端记为已触发，不依赖 AI 回显。

---

## 🟡 四类：其它

**F11. prompt 太重（todolist #26）** —— 攻略 prompt 很长，容易超时/漏格。碎片剧场已经是精简版；日常互动也可以走更轻的 prompt。

**F12. 宝妈/CP 死代码（todolist #28）** —— 建号已不暴露这两个模式，相关分支是死代码，留着增加维护面和 bug 面。
→ 修：清理（确认无存档依赖后）。

**F13. 内联"剧情"聊天页的选项是只读的** —— 那个视图里 A/B/C 只是回顾、不可点，靠底部输入框。容易让人以为"点了没反应"。
→ 修：要么让它们可点，要么去掉只读选项、只留输入框。

---

## 建议动手顺序

1. **先堵时间/世界所有权（F2–F5）** —— 这是你最在意的"按时刻发生"，也是"没捋顺"的病根。一次把地点、时钟、回归期、打歌名次的所有权收回给游戏。
2. **再修状态可靠性（F8–F10）** —— 让好感/曝光/里程碑不再靠 AI 回显，玩久了不崩。
3. **松绑照看循环（F6–F7）** —— 让"逐个照看需求"顺畅。
4. **清理（F12）+ 轻量化（F11）+ 小 UI（F13）** 收尾。
